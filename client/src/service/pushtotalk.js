import './styles.css'

export const Pushtotalk = ({ socket }) => {
    return (
        <div>
            <button className='btn-push-to-talk'>push</button>
        </div>
    )
}

export default Pushtotalk